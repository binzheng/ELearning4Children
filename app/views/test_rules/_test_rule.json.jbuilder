json.extract! test_rule, :id, :ruleUserId, :mTestTypeId, :testCount, :maxScore, :maxPoint, :minScore, :minPoint, :midScore, :midPoint, :created_at, :updated_at
json.url test_rule_url(test_rule, format: :json)
